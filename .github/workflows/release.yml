name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: pomodorust

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: pomodorust-linux-x64
            binary: pomodorust
            use_upx: true
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: pomodorust-windows-x64
            binary: pomodorust.exe
            use_upx: true
          - os: macos-15
            target: x86_64-apple-darwin
            artifact: pomodorust-macos-x64
            binary: pomodorust
            use_upx: false
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: pomodorust-macos-arm64
            binary: pomodorust
            use_upx: false

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          targets: ${{ matrix.target }}
          components: rust-src

      - uses: Swatinem/rust-cache@v2

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev libxkbcommon-dev libssl-dev libasound2-dev

      - name: Build with nightly + build-std
        run: cargo +nightly build --release -Z build-std=std,panic_abort --target ${{ matrix.target }}

      - name: Strip binary (Linux/macOS)
        if: matrix.os != 'windows-latest'
        run: strip target/${{ matrix.target }}/release/${{ matrix.binary }}

      - name: Install UPX (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get install -y upx

      - name: Install UPX (Windows)
        if: matrix.os == 'windows-latest'
        run: choco install upx -y

      - name: Compress with UPX
        if: matrix.use_upx
        shell: bash
        run: |
          upx --best --lzma target/${{ matrix.target }}/release/${{ matrix.binary }} || true

      - name: Create archive (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          cd target/${{ matrix.target }}/release
          tar -czvf ../../../${{ matrix.artifact }}.tar.gz ${{ matrix.binary }}

      - name: Create archive (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../${{ matrix.artifact }}.zip ${{ matrix.binary }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.tar.gz
            ${{ matrix.artifact }}.zip

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          cd artifacts
          find . -type f \( -name "*.tar.gz" -o -name "*.zip" \) -exec mv {} . \;
          sha256sum *.tar.gz *.zip > checksums.txt

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: PomodoRust ${{ steps.version.outputs.version }}
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc') }}
          files: |
            artifacts/*.tar.gz
            artifacts/*.zip
            artifacts/checksums.txt
          body: |
            ## PomodoRust - Modern Pomodoro Timer

            Lightweight, single-file Pomodoro timer with Vercel-style dark UI.

            ### Downloads
            | Platform | File |
            |----------|------|
            | Windows x64 | `pomodorust-windows-x64.zip` |
            | Linux x64 | `pomodorust-linux-x64.tar.gz` |
            | macOS Intel | `pomodorust-macos-x64.tar.gz` |
            | macOS Apple Silicon | `pomodorust-macos-arm64.tar.gz` |

            ### Features
            - Pomodoro, short break, long break timers
            - Custom presets
            - Session statistics with SQLite
            - System notifications
            - Audio alerts (generated, no external files)
            - Single executable, no dependencies

            ### Checksums
            See `checksums.txt` for SHA256 hashes.
