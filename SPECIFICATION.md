# PomodoRust - Спецификация приложения

## Обзор проекта

**Название:** PomodoRust
**Тип:** Lightweight Desktop Pomodoro Timer
**Платформа:** Windows (с потенциалом кросс-платформенности)
**Стиль:** Vercel/shadcn-inspired dark UI с градиентами и анимациями

---

## 1. Технический стек

### Основные технологии
| Компонент | Технология | Обоснование |
|-----------|------------|-------------|
| Язык | Rust 2021 edition | Производительность, безопасность, малый бинарник |
| GUI Framework | eframe/egui (glow backend) | Легковесность (~3-4 MB), кастомизация UI |
| База данных | SQLite (rusqlite) | Статистика, быстрые запросы |
| Конфигурация | TOML (serde + toml) | Читаемость, Rust-идиоматичность |
| Аудио | rodio | Кроссплатформенность, простота |
| Windows API | windows-rs | Нативные уведомления, автозапуск, tray |
| Анимации | egui_extras + custom | Плавные переходы |

### Cargo.toml зависимости
```toml
[package]
name = "pomodorust"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.29", default-features = false, features = ["glow"] }
egui = "0.29"
egui_extras = { version = "0.29", features = ["image"] }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
rodio = "0.19"
directories = "5.0"
chrono = { version = "0.4", features = ["serde"] }
image = "0.25"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Foundation",
    "Win32_System_Com",
] }
winreg = "0.52"
notify-rust = "4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
```

---

## 2. Архитектура приложения

### Структура проекта
```
pomodorust/
├── Cargo.toml
├── build.rs              # Windows resource embedding
├── assets/
│   ├── icon.ico          # Иконка приложения
│   ├── icon.png          # Для tray
│   └── sounds/
│       ├── work_end.wav
│       ├── break_end.wav
│       ├── tick.wav
│       └── notification.wav
├── src/
│   ├── main.rs           # Entry point, window setup
│   ├── app.rs            # Main App struct, UI orchestration
│   ├── lib.rs            # Library exports
│   │
│   ├── core/
│   │   ├── mod.rs
│   │   ├── timer.rs      # Pomodoro timer logic
│   │   ├── session.rs    # Session management (work/break/long break)
│   │   └── preset.rs     # Timer presets
│   │
│   ├── ui/
│   │   ├── mod.rs
│   │   ├── theme.rs      # Color palette, gradients, fonts
│   │   ├── titlebar.rs   # Custom window titlebar
│   │   ├── timer_view.rs # Compact timer widget
│   │   ├── dashboard.rs  # Full statistics dashboard
│   │   ├── settings.rs   # Settings panel
│   │   ├── todo.rs       # Todo list (future)
│   │   ├── components/
│   │   │   ├── mod.rs
│   │   │   ├── button.rs
│   │   │   ├── progress.rs    # Circular progress
│   │   │   ├── chart.rs       # Statistics charts
│   │   │   └── card.rs
│   │   └── animations.rs # Animation utilities
│   │
│   ├── data/
│   │   ├── mod.rs
│   │   ├── database.rs   # SQLite operations
│   │   ├── config.rs     # TOML config management
│   │   └── models.rs     # Data structures
│   │
│   ├── platform/
│   │   ├── mod.rs
│   │   ├── windows.rs    # Windows-specific (tray, autostart, notifications)
│   │   └── audio.rs      # Sound playback
│   │
│   └── utils/
│       ├── mod.rs
│       └── time.rs       # Time formatting utilities
│
└── resources/
    └── app.rc            # Windows resources
```

### Модульная архитектура (Diagram)
```
┌─────────────────────────────────────────────────────────────┐
│                        UI Layer                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ TitleBar │ │TimerView │ │Dashboard │ │ Settings │        │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘        │
│       │            │            │            │               │
│       └────────────┴────────────┴────────────┘               │
│                         │                                    │
│  ┌──────────────────────┴──────────────────────┐            │
│  │              App State (egui)                │            │
│  └──────────────────────┬──────────────────────┘            │
└──────────────────────────┼───────────────────────────────────┘
                           │
┌──────────────────────────┼───────────────────────────────────┐
│                    Core Layer                                │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                        │
│  │  Timer  │ │ Session │ │ Preset  │                        │
│  └────┬────┘ └────┬────┘ └────┬────┘                        │
└───────┼───────────┼───────────┼──────────────────────────────┘
        │           │           │
┌───────┼───────────┼───────────┼──────────────────────────────┐
│       │      Data Layer       │                              │
│  ┌────┴────┐ ┌────┴────┐ ┌────┴────┐                        │
│  │ SQLite  │ │  TOML   │ │ Models  │                        │
│  │   DB    │ │ Config  │ │         │                        │
│  └─────────┘ └─────────┘ └─────────┘                        │
└──────────────────────────────────────────────────────────────┘
        │
┌───────┴──────────────────────────────────────────────────────┐
│                   Platform Layer                             │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│  │  Tray   │ │AutoStart│ │  Audio  │ │  Toast  │            │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘            │
└──────────────────────────────────────────────────────────────┘
```

---

## 3. Дизайн-система (Vercel/shadcn стиль)

### Цветовая палитра
```rust
pub struct Theme {
    // Backgrounds
    pub bg_primary: Color32,      // #0A0A0B - Глубокий чёрный
    pub bg_secondary: Color32,    // #111113 - Карточки
    pub bg_tertiary: Color32,     // #18181B - Hover состояния
    pub bg_elevated: Color32,     // #1C1C1F - Модальные окна

    // Borders
    pub border_subtle: Color32,   // #27272A - Тонкие границы
    pub border_default: Color32,  // #3F3F46 - Обычные границы

    // Text
    pub text_primary: Color32,    // #FAFAFA - Основной текст
    pub text_secondary: Color32,  // #A1A1AA - Вторичный текст
    pub text_muted: Color32,      // #71717A - Приглушённый

    // Accent (градиенты)
    pub accent_start: Color32,    // #3B82F6 - Синий
    pub accent_end: Color32,      // #8B5CF6 - Фиолетовый

    // Semantic
    pub success: Color32,         // #22C55E - Зелёный
    pub warning: Color32,         // #F59E0B - Оранжевый
    pub error: Color32,           // #EF4444 - Красный

    // Timer states
    pub work_color: Color32,      // #F43F5E - Работа (rose)
    pub break_color: Color32,     // #10B981 - Перерыв (emerald)
    pub long_break_color: Color32,// #6366F1 - Длинный перерыв (indigo)
}
```

### Градиенты
```rust
pub enum Gradient {
    // Главный акцент (кнопки, прогресс)
    Primary,     // #3B82F6 → #8B5CF6 (blue → violet)

    // Таймер в режиме работы
    Work,        // #F43F5E → #FB7185 (rose)

    // Таймер в режиме перерыва
    Break,       // #10B981 → #34D399 (emerald)

    // Длинный перерыв
    LongBreak,   // #6366F1 → #818CF8 (indigo)

    // Статистика
    Stats,       // #F59E0B → #FBBF24 (amber)

    // Subtle background glow
    Glow,        // радиальный, полупрозрачный
}
```

### Типографика
```rust
pub struct Typography {
    pub font_family: &'static str,  // Inter или system-ui

    // Sizes
    pub display: f32,    // 48px - Таймер
    pub h1: f32,         // 32px - Заголовки
    pub h2: f32,         // 24px - Подзаголовки
    pub body: f32,       // 16px - Основной текст
    pub small: f32,      // 14px - Вторичный текст
    pub caption: f32,    // 12px - Подписи

    // Weights
    pub regular: FontWeight,   // 400
    pub medium: FontWeight,    // 500
    pub semibold: FontWeight,  // 600
    pub bold: FontWeight,      // 700
}
```

### Spacing & Rounding
```rust
pub struct Spacing {
    pub xs: f32,    // 4px
    pub sm: f32,    // 8px
    pub md: f32,    // 16px
    pub lg: f32,    // 24px
    pub xl: f32,    // 32px
    pub xxl: f32,   // 48px
}

pub struct Rounding {
    pub none: f32,    // 0px
    pub sm: f32,      // 4px
    pub md: f32,      // 8px
    pub lg: f32,      // 12px
    pub xl: f32,      // 16px
    pub full: f32,    // 9999px (круг)

    pub window: f32,  // 12px - Углы окна
    pub card: f32,    // 12px - Карточки
    pub button: f32,  // 8px - Кнопки
}
```

---

## 4. UI/UX Компоненты

### 4.1 Кастомный Title Bar
```
┌──────────────────────────────────────────────────────────────┐
│  ●  PomodoRust                              ─  □  ✕  │
│     ↑ logo                                   min max close   │
└──────────────────────────────────────────────────────────────┘
```
- Высота: 40px
- Drag для перемещения окна
- Double-click для maximize/restore
- Кнопки с hover анимацией
- Close: hover → красный

### 4.2 Компактный виджет (Timer View)
```
┌────────────────────────────────────────┐
│  [Title Bar]                           │
├────────────────────────────────────────┤
│                                        │
│        ┌─────────────────┐             │
│        │                 │             │
│        │     25:00       │ ← circular  │
│        │                 │   progress  │
│        │    FOCUS        │             │
│        └─────────────────┘             │
│                                        │
│    [ ▶ Start ]  [ ⏭ Skip ]            │
│                                        │
│    Session: 1/4   ●●○○                │
│                                        │
│    [≡ Dashboard]  [⚙ Settings]        │
└────────────────────────────────────────┘
```
Размер: 320x400 px

### 4.3 Dashboard (расширенный вид)
```
┌──────────────────────────────────────────────────────────────┐
│  [Title Bar]                                                 │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────┐  ┌─────────────────────────────────────────┐│
│  │   Timer    │  │           Statistics Cards              ││
│  │            │  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   ││
│  │   25:00    │  │  │Today │ │Week  │ │Streak│ │Total │   ││
│  │   FOCUS    │  │  │ 4h   │ │ 20h  │ │ 7🔥  │ │ 120h │   ││
│  │            │  │  └──────┘ └──────┘ └──────┘ └──────┘   ││
│  │ [▶] [⏭]  │  │                                         ││
│  └────────────┘  │  ┌─────────────────────────────────────┐││
│                  │  │      Weekly Activity Chart          │││
│  ┌────────────┐  │  │  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  │││
│  │ Quick      │  │  │  Mon Tue Wed Thu Fri Sat Sun        │││
│  │ Actions    │  │  └─────────────────────────────────────┘││
│  │            │  │                                         ││
│  │ ☕ 5min    │  │  ┌─────────────────────────────────────┐││
│  │ 🍅 25min   │  │  │      Today's Sessions Timeline     │││
│  │ 📚 50min   │  │  │  09:00 ▓▓▓▓ 10:30 ▓▓ 14:00 ▓▓▓▓    │││
│  └────────────┘  │  └─────────────────────────────────────┘││
│                  └─────────────────────────────────────────┘│
│                                                              │
│  [🍅 Timer]  [📊 Stats]  [✓ Tasks]  [⚙ Settings]          │
└──────────────────────────────────────────────────────────────┘
```
Размер: 800x600 px (resize enabled)

### 4.4 Settings Panel
```
┌────────────────────────────────────────────────────────────┐
│  ← Back        Settings                                    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ▼ Timer                                                   │
│    ┌────────────────────────────────────────────────────┐  │
│    │  Focus Duration        [  25  ] min     [-] [+]   │  │
│    │  Short Break           [   5  ] min     [-] [+]   │  │
│    │  Long Break            [  15  ] min     [-] [+]   │  │
│    │  Sessions before long  [   4  ]         [-] [+]   │  │
│    │                                                    │  │
│    │  Auto-start breaks     [====○      ]              │  │
│    │  Auto-start pomodoros  [      ○====]              │  │
│    └────────────────────────────────────────────────────┘  │
│                                                            │
│  ▼ Sounds                                                  │
│    ┌────────────────────────────────────────────────────┐  │
│    │  Volume                [========●==]  80%         │  │
│    │  Work End Sound        [ Bell ▼ ]       [▶ Test]  │  │
│    │  Break End Sound       [ Chime ▼ ]      [▶ Test]  │  │
│    │  Tick Sound            [○ Disabled]               │  │
│    └────────────────────────────────────────────────────┘  │
│                                                            │
│  ▼ Appearance                                              │
│    ┌────────────────────────────────────────────────────┐  │
│    │  Accent Color          [● Blue] [○ Purple] [○ ...] │  │
│    │  Timer Gradient        [● Work ] [○ Break ] [○ ...]│  │
│    │  Compact mode          [====○      ]              │  │
│    └────────────────────────────────────────────────────┘  │
│                                                            │
│  ▼ System                                                  │
│    ┌────────────────────────────────────────────────────┐  │
│    │  Start with Windows    [====○      ]              │  │
│    │  Minimize to tray      [====○      ]              │  │
│    │  Show in taskbar       [====○      ]              │  │
│    └────────────────────────────────────────────────────┘  │
│                                                            │
│  ▼ Presets                                                 │
│    ┌────────────────────────────────────────────────────┐  │
│    │  [Classic 25/5/15] [Short 15/3/10] [Long 50/10/30]│  │
│    │  [+ Create Custom Preset]                          │  │
│    └────────────────────────────────────────────────────┘  │
│                                                            │
│                              [Reset to Defaults]           │
└────────────────────────────────────────────────────────────┘
```

---

## 5. Анимации

### Типы анимаций
| Анимация | Длительность | Easing | Применение |
|----------|--------------|--------|------------|
| Hover | 150ms | ease-out | Кнопки, карточки |
| Click | 100ms | ease-in-out | Нажатие кнопки |
| Page transition | 300ms | ease-in-out | Переключение views |
| Progress | 16ms tick | linear | Круговой прогресс |
| Pulse | 2000ms | ease-in-out | Активный таймер |
| Fade | 200ms | ease-out | Появление элементов |
| Scale | 150ms | spring | Hover на карточках |
| Glow | 3000ms | sine | Фоновое свечение |

### Ключевые анимации
1. **Circular Progress** - плавное заполнение/убывание кольца таймера
2. **Pulse Effect** - пульсация таймера во время работы
3. **Button Hover** - subtle scale + glow
4. **Card Hover** - lift effect (translate-y + shadow)
5. **Page Slide** - slide + fade между views
6. **Chart Animation** - появление графиков с анимацией
7. **Number Counter** - плавный счёт статистики

---

## 6. Статистика и данные

### SQLite Schema
```sql
-- Сессии pomodoro
CREATE TABLE sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_type TEXT NOT NULL, -- 'work', 'break', 'long_break'
    duration_seconds INTEGER NOT NULL,
    planned_duration INTEGER NOT NULL,
    completed BOOLEAN NOT NULL,
    started_at DATETIME NOT NULL,
    ended_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Дневная статистика (агрегация)
CREATE TABLE daily_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date DATE UNIQUE NOT NULL,
    total_work_seconds INTEGER DEFAULT 0,
    total_break_seconds INTEGER DEFAULT 0,
    completed_pomodoros INTEGER DEFAULT 0,
    interrupted_pomodoros INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Streak tracking
CREATE TABLE streaks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    current_streak INTEGER DEFAULT 0,
    longest_streak INTEGER DEFAULT 0,
    last_active_date DATE,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Пресеты таймера
CREATE TABLE presets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    work_duration INTEGER NOT NULL,
    short_break INTEGER NOT NULL,
    long_break INTEGER NOT NULL,
    sessions_before_long INTEGER NOT NULL,
    is_default BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Индексы для быстрых запросов
CREATE INDEX idx_sessions_date ON sessions(started_at);
CREATE INDEX idx_daily_stats_date ON daily_stats(date);
```

### Статистика для отображения
1. **Сегодня:** завершённые помодоро, общее время фокуса
2. **Неделя:** график по дням, сравнение с прошлой неделей
3. **Streak:** текущий и рекордный streak
4. **All-time:** общие часы, всего помодоро
5. **Heatmap:** активность за месяц (future)

---

## 7. Конфигурация (TOML)

### config.toml
```toml
[timer]
work_duration = 25        # минуты
short_break = 5           # минуты
long_break = 15           # минуты
sessions_before_long = 4
auto_start_breaks = false
auto_start_work = false

[sounds]
enabled = true
volume = 80               # 0-100
work_end = "bell"         # встроенный звук или путь к файлу
break_end = "chime"
tick_enabled = false
tick_sound = "tick"

[appearance]
accent_color = "blue"     # blue, purple, rose, emerald
theme = "dark"            # dark only for now
compact_mode = false
window_opacity = 100      # 80-100

[system]
start_with_windows = false
minimize_to_tray = true
show_in_taskbar = true
notifications_enabled = true

[window]
width = 320
height = 400
x = null                  # null = centered
y = null
always_on_top = false
```

---

## 8. Windows интеграция

### System Tray
- Иконка с динамическим статусом (работа/перерыв/пауза)
- Контекстное меню:
  - Start/Pause
  - Skip
  - Settings
  - ─────────
  - Show/Hide Window
  - Quit

### Уведомления
- Native Windows Toast notifications
- Action buttons: "Start Break" / "Skip Break"
- Звук синхронизирован с toast

### Автозапуск
- Добавление в `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`
- Опция "Start minimized"

### Single Instance
- Mutex для одного экземпляра
- Повторный запуск активирует существующее окно

---

## 9. Keyboard Shortcuts

| Shortcut | Action |
|----------|--------|
| `Space` | Start/Pause timer |
| `S` | Skip current session |
| `R` | Reset current timer |
| `D` | Toggle Dashboard |
| `Ctrl+,` | Open Settings |
| `Escape` | Minimize to tray |
| `Ctrl+Q` | Quit app |
| `1-4` | Quick presets |

---

## 10. Roadmap разработки

### Phase 1: Core MVP
- [ ] Базовая структура проекта
- [ ] Timer core logic
- [ ] Простой UI с egui
- [ ] Кастомный title bar
- [ ] Базовые настройки

### Phase 2: Polish
- [ ] Gradient & animations
- [ ] SQLite статистика
- [ ] Dashboard с графиками
- [ ] System tray
- [ ] Windows notifications

### Phase 3: Features
- [ ] Sound system
- [ ] Presets
- [ ] Autostart
- [ ] Keyboard shortcuts
- [ ] Custom themes

### Phase 4: Future (Todo Integration)
- [ ] Todo list с интеграцией в pomodoro
- [ ] Привязка задачи к сессии
- [ ] Статистика по задачам

---

## 11. Примечания для реализации

### Градиенты в egui
egui не поддерживает нативные градиенты. Решения:
1. **Mesh градиенты** - через `epaint::Mesh` с вершинными цветами
2. **Shader** - кастомный GLSL shader через glow
3. **Многослойность** - несколько полупрозрачных слоёв

Рекомендуется: Mesh градиенты для круглого прогресса, шейдер для фоновых эффектов.

### Circular Progress
Использовать `epaint::Shape::Path` с arc сегментами и mesh для градиента.

### Smooth Animations
egui работает через repaint-on-demand. Для анимаций:
```rust
ctx.request_repaint(); // Постоянная перерисовка для анимации
```

### Inter Font
Встроить subset Inter (Regular, Medium, SemiBold) через `include_bytes!()`.

---

## 12. Ассеты (требуются)

### Обязательные
- [ ] `icon.ico` - 256x256, multi-resolution
- [ ] `icon.png` - 64x64 для tray
- [ ] Звуки (можно сгенерировать или использовать CC0):
  - [ ] `work_end.wav`
  - [ ] `break_end.wav`
  - [ ] `notification.wav`

### Опциональные (можно сгенерировать программно)
- Tray статус иконки (работа/перерыв/пауза)
- Placeholder для графиков
